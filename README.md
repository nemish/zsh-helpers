Zsh commands and aliases which I found helpful.

Usage:

```
cd ~/path/to/projects
git clone git@github.com:nemish/zsh-helpers.git
echo "source ~/path/to/projects/zsh-helpers/.zsh-helpers" >> ~/.zshrc && source ~/.zshrc
```

Enjoy :)
